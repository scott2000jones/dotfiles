{
    "layer": "top",
    "modules-left": ["sway/window","sway/mode"],
    "modules-center": [],
    "modules-right": ["battery", "clock"],
    "sway/window": {
        "max-length": 50
    },
    "battery": {
        "format": "{capacity}% {icon}",
        "format-icons": ["W", "A", "S", "D", "Q"]
    },
    "clock": {
        "format": "{:%a %H:%M}"
    }
}
